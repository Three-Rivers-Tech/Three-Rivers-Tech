# Local development environment variables (copy to `env.local` and fill in real values; NEVER commit secrets)

# Core site config
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Google Analytics 4 Measurement ID (format: G-XXXXXXXXXX)
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Sentry DSN (rotate if compromised; do not commit the real value)
SENTRY_DSN=your-sentry-dsn-here

# Optional Slack webhook for error/alert notifications
SLACK_WEBHOOK_URL=your-slack-webhook-url-here

# Add any other public-only Next.js vars with NEXT_PUBLIC_ prefix below
# NEXT_PUBLIC_EXAMPLE_FEATURE_FLAG=false
